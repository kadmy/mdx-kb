# MDX Sandbox - Live Preview Editor

Редактор MDX с live preview и runtime compilation для базы знаний.

## Ключевые возможности (v2.0)

### ✅ UX улучшения

1. **Runtime MDX Compilation** ⭐
   - Живой предпросмотр при редактировании
   - Автоматическая компиляция с debounce (500ms)
   - Показ ошибок компиляции прямо в UI
   - Использует `@mdx-js/mdx` evaluate() для динамической компиляции
   - Компоненты передаются через scope с `useMDXComponents`

2. **Индикация несохраненных изменений**
   - Визуальный индикатор "● Есть несохраненные изменения"
   - Кнопка "Сохранить *" меняет цвет при наличии изменений
   - Предупреждение при закрытии вкладки с несохраненными данными

3. **Валидация YAML и MDX**
   - Проверка синтаксиса YAML frontmatter
   - Проверка обязательных полей (title)
   - Показ ошибок и предупреждений в отдельной панели
   - Блокировка сохранения при наличии ошибок

4. **Настоящие Tooltips** 🎯
   - Заменен `title` attribute на полноценный Tooltip компонент
   - Следует за курсором мыши
   - Поддерживает форматирование
   - Используется в компоненте `<Term>` для показа определений терминов
   - Визуальная подсветка терминов: фиолетовый пунктир + cursor: help

5. **ErrorBoundary для preview**
   - Перехват ошибок рендеринга
   - Красивое отображение ошибок компиляции
   - Показ номера строки и колонки ошибки

6. **Единый dev-сервер**
   - Убран отдельный Express сервер
   - API endpoints встроены в Vite middleware
   - Запуск одной командой: `npm run dev`

## Запуск

```bash
# Установка зависимостей
npm install

# Запуск dev-сервера (порт 3000)
npm run dev

# Сборка для production
npm build

# Предпросмотр production build
npm run preview
```

## Структура проекта

```
mdx-sandbox/
├── src/
│   ├── App.jsx                   # Главный компонент с runtime compilation
│   ├── index.jsx                 # Точка входа
│   ├── content.mdx               # Редактируемый MDX файл
│   ├── components/
│   │   ├── concept.jsx           # Компонент для концепций
│   │   ├── term.jsx              # Компонент для терминов (с Tooltip)
│   │   ├── Glossary.jsx          # Автогенерация словаря
│   │   ├── Modal.jsx             # Модальное окно
│   │   ├── ErrorBoundary.jsx     # Обработка ошибок
│   │   ├── ValidationPanel.jsx   # Панель валидации
│   │   └── Tooltip.jsx           # Настоящий tooltip
│   ├── services/
│   │   ├── mdx-compiler.js       # Runtime MDX compilation
│   │   └── validator.js          # Валидация YAML/MDX
│   └── utils/
│       └── debounce.js           # Утилита debounce
├── vite.config.mjs               # Vite config с API endpoints
└── package.json
```

## API Endpoints (встроены в Vite)

- `GET /api/load` - загрузка MDX файла
- `POST /api/save` - сохранение MDX файла

## Использование

1. Открыть http://localhost:3000
2. Редактировать YAML frontmatter в верхнем поле
3. Редактировать MDX контент в нижнем поле
4. Видеть live preview справа
5. Сохранить изменения кнопкой "Сохранить"

## Компоненты доступные в MDX

**ВАЖНО:** Используйте PascalCase для всех компонентов в MDX!

```mdx
<!-- Правильно ✅ -->
<Concept id="zamysel-vsevyshnego">Замысел</Concept>
<Term term="логос">Логос</Term>
<Glossary terms={['афар', 'логос', 'иври']} />

<!-- Неправильно ❌ - не будет работать -->
<concept id="...">...</concept>
<term term="...">...</term>
```

**Почему PascalCase?** MDX распознает только PascalCase теги как компоненты. Lowercase теги (`<term>`, `<concept>`) интерпретируются как обычные HTML элементы.

## Следующие шаги (для интеграции в solid-version)

1. Form-based редактор frontmatter (вместо textarea)
2. Система концепций с реальными данными (вместо заглушек)
3. Фасетная структура данных (speakers, series, topics)
4. Monaco Editor для подсветки синтаксиса

## Миграция с v1.0

**Что изменилось:**
- Удален `server.js` (Express)
- Удалены зависимости: `express`, `cors`
- Удален script `npm run serve`
- API теперь на том же порту что и dev-сервер (3000)

**Как мигрировать:**
1. `npm install` - переустановить зависимости
2. Использовать только `npm run dev` (без `npm run serve`)